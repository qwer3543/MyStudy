
public class Ex05_max_number {

	public static void main(String[] args) {
		// 숫자 3가지 중 가장 큰수를 구하시오
		//a: 30 , b:60 , c: 20
		//결과 -> 가장큰수 :60
		
		int a = 70;
		int b = 60;
		int c = 10;
		
		if (a>b && a>c ) {
			System.out.println("가장큰수 : " + a);
		}else if (b>a && b>c){
			System.out.println("가장큰수 : " + b);
		}else {
			System.out.println("가장큰수 : " + c);
		}
		
		//===========================================
		System.out.println("=====================================");
		
		if (a > b) {
			if (a >c) {
				System.out.println("가장큰수 : " + a);
			}
		} else {
			if (b > a) {
				if (b >c) {
					System.out.println("가장큰수 : " + b);
				} else {
					System.out.println("가장큰수 : " + c);
				}
			}
		}
		
		//====================================================
		System.out.println("=====================================");
				
		// a: 30 , b: 60 , c: 20 , d: 15 , e: 70
		// 가장 큰수 계산
		// 결과 => 가장큰수: ??
		
		int a1 = 70;
		int b1 = 60;
		int c1 = 10;
		int d1 = 10;
		int e1 = 10;
		int max = -2147483648; //Integer.MIN_VALUE; // max = -2147483648;
		
		if (a1 > max ) {
			max = a1;
		}
		if (b1 > max ) {
			max = b1;
		}
		
		System.out.println(max);
		
		
		
		
		
		

	}

}
